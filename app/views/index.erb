<ul id="links">
	<% @links.each do |link| %>
		<%= partial :link, :locals => {:link => link}  %>
	<% end %>
</ul>

<h3>Available Tags</h3>
|<% @available_tags.each do |tag| %>
	<a href="/tags/<%= tag.text %>"><%= tag.text.capitalize %></a> |
<% end %>